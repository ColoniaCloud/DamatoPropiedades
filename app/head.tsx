// Los meta tags son gestionados por `export const metadata` en layout.tsx y generateMetadata en cada página.
// Este archivo se mantiene vacío para evitar duplicados.
export default function Head() {
  return null;
}

